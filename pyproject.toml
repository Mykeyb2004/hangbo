[project]
name = "hangbo-survey-stats"
version = "0.1.0"
description = "Survey statistics scripts for exhibition reports"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.30.0",
    "openpyxl>=3.1.5",
    "pandas>=2.2.3",
    "python-pptx>=1.0.2",
]
