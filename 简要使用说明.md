# 简要使用说明

这个 repo 当前推荐顺序是：

1. 把 5 月原始问卷放到  
   [data/raw/2026/5月](/Users/zhangqijin/PycharmProjects/hangbo/data/raw/2026/5月)

2. 先跑单月主流程：

```bash
uv run python main_pipeline.py --year 2026 --batch 5月
```

这一步会按顺序做：

```text
预查错
-> 必要时自动补 年份/月份
-> 满意度分项统计
-> 满意度汇总
-> 样本汇总
-> PPT
```

代码入口在 [main_pipeline.py](/Users/zhangqijin/PycharmProjects/hangbo/main_pipeline.py)，实际顺序在 [runtime.py](/Users/zhangqijin/PycharmProjects/hangbo/src/hangbo/pipeline/runtime.py)。

3. 如果之后要做累计批次，比如 `1-5月`，才做合并。合并前要保证各单月数据已经有正确的 `年份` / `月份`：

```bash
uv run python -m hangbo.merge.questionnaire_workbooks \
  --input-dir 'data/raw/2026/1-2月' \
  --input-dir 'data/raw/2026/3月' \
  --input-dir 'data/raw/2026/4月' \
  --input-dir 'data/raw/2026/5月' \
  --output-dir 'data/raw/2026/1-5月'
```

然后再跑累计批次：

```bash
uv run python main_pipeline.py --year 2026 --batch 1-5月
```

关键点：`1-5月`、`Q1`、`1-4月` 这种合并批次，程序**不会自动补年月**，因为它不知道每一行属于哪个自然月。合并脚本也只是表头对齐和行追加，不会去重、修标签或判断记录新旧。

所以简化成一句话就是：

```text
5月原始数据 -> 先跑5月主流程/清洗预查错 -> 需要累计时再合并 -> 跑累计批次主流程
```

你说“需要先做数据清洗”是对的，尤其是年月列、期次列、未映射客户标签这些问题，最好在单月阶段先拦住。