# 数据分析使用手册

这份手册只保留日常操作需要的流程和命令。

项目统一使用 `uv` 运行：

```bash
uv run python ...
```

---

## 0. 每月例行用例

每个月通常要做两件事：

```text
计算当月样本总数和满意度
更新 1-x 月累计样本总数和满意度
```

例如现在有 6月新数据，推荐按这个顺序操作：

### 第一步：清洗并计算 6 月

先把 6 月原始 Excel 放到：

```text
data/raw/2026/5月
```

然后运行：

```bash
uv run python main_pipeline.py --year 2026 --batch 6月
```

这一步会自动做预查错、期次列兼容、单月 `年份` / `月份` 补写，并输出 6月的：

```text
样本统计表
满意度分项统计
满意度汇总表
PPT
```

### 第二步：合并成 1-6 月

确认 6 月结果没问题后，把历史累计批次和 6 月合并：

```bash
uv run python -m hangbo.merge.questionnaire_workbooks \
  --input-dir 'data/raw/2026/1-2月' \
  --input-dir 'data/raw/2026/3月' \
  --input-dir 'data/raw/2026/4月' \
  --input-dir 'data/raw/2026/5月' \
  --input-dir 'data/raw/2026/6月' \
  --output-dir 'data/raw/2026/1-6月'
```

这一步只是把多个月的原始问卷合并到：

```text
data/raw/2026/1-6月
```

### 第三步：计算 1-6 月累计样本和满意度

合并完成后，再运行累计批次主流程：

```bash
uv run python main_pipeline.py --year 2026 --batch 1-6月
```

这一步会输出 1-6月累计的：

```text
样本统计表
满意度分项统计
满意度汇总表
PPT
```

### 这个用例里最容易遗漏的点

- 先跑 6月`，再合并 `1-6月`。
- 单月批次会自动补 `年份` / `月份`，累计批次不会。
- 合并前要确认来源目录中的记录已经有正确的 `年份` / `月份`。
- 合并脚本只负责追加行，不负责去重、修标签或判断新旧记录。
- 如果只需要累计样本统计表，不需要满意度和 PPT，可以用 `uv run python merge_sample_summary.py --year 2026`。

---

## 1. 目录放置规则

原始数据放在：

```text
data/raw/{年份}/{批次}
```

例如：

```text
data/raw/2026/5月
data/raw/2026/1-5月
data/raw/2026/Q1
```

每个批次目录里放标准来源文件，文件名通常是：

```text
展览.xlsx
会议.xlsx
酒店.xlsx
餐饮.xlsx
会展服务商.xlsx
旅游.xlsx
```

说明：

- 某个月没有某类数据，可以不放对应文件。
- 但一个批次目录里至少要有一个标准来源文件。
- 脚本默认读取 Excel 里的 `问卷数据` sheet。

---

## 2. 最常用：处理单月数据

例如处理 2026 年 5 月：

1. 把 5 月原始 Excel 放到：

```text
data/raw/2026/5月
```

2. 运行主流程：

```bash
uv run python main_pipeline.py --year 2026 --batch 5月
```

主流程会自动执行：

```text
预查错
-> 必要时人工修改原始数据
-> 单月自动补写 年份/月份
-> 满意度分项统计
-> 满意度汇总表
-> 样本统计表
-> PPT 生成前确认
-> PPT
```

生成 PPT 前，程序会停下来询问是否继续：

```text
是否继续生成 PPT？输入 y / yes / 继续；输入 stop 终止：
```

输入 `y`、`yes`、`继续` 会继续生成 PPT。
输入 `stop`、`quit`、`exit` 会终止，不生成 PPT。

---

## 3. 输出结果在哪里

以 `2026/5月` 为例：

```text
data/satisfaction_detail/2026/5月      满意度分项统计
data/satisfaction_summary/2026/5月     满意度汇总表
data/sample_summary/2026/5月           样本统计表
data/ppt/2026/5月                      PPT
logs/pipeline/2026/5月                 运行日志和预查错日志
```

主要文件一般是：

```text
data/satisfaction_summary/2026/5月/5月客户类型满意度汇总表.xlsx
data/sample_summary/2026/5月/5月客户类型样本统计表.xlsx
data/ppt/2026/5月/5月满意度报告.pptx
```

---

## 4. 清洗数据和预查错

主流程一开始会先做预查错。这里的“清洗/兼容”主要包括：

- 兼容新版问卷的期次列：如果 `问卷数据` 第三列是 `一期`、`二期`、`三期` 等，会自动把这一列移动到最后。
- 单月批次自动补写 `年份` / `月份`。
- 检查是否有未映射客户标签记录。
- 检查是否缺少 `问卷数据` sheet。
- 检查 Excel 是否能正常读取。

如果发现阻断问题，程序会停下来，让你先修改原始数据。修改完成后，在终端输入：

```text
y
yes
继续
```

即可重新检查并继续。

如果要终止，输入：

```text
stop
quit
exit
```

需要看的日志：

```text
logs/pipeline/{年份}/{批次}/precheck.log
logs/pipeline/{年份}/{批次}/unmapped_customer_records.log
logs/pipeline/{年份}/{批次}/pipeline.log
```

注意：脚本不会自动去重、不会自动修正客户标签，也不会自动判断哪条记录应该保留。这些需要根据日志人工改原始 Excel。

---

## 5. 年份 / 月份列规则

### 单月批次

`1月` 到 `12月` 这种批次，主流程能识别为单月。

例如：

```bash
uv run python main_pipeline.py --year 2026 --batch 5月
```

如果 `data/raw/2026/5月` 里缺少 `年份` / `月份` 列，程序会自动补：

```text
年份 = 2026
月份 = 5
```

### 合并批次

`1-5月`、`Q1` 这类批次可能包含多个月份，程序不能判断每一行属于哪个月。

所以合并批次必须提前准备好正确的 `年份` / `月份` 列。否则主流程会阻断。

### 手动补写年月

通常不需要手动跑；主流程会给单月批次自动补。

如果确实要手动补单月目录，可以运行：

```bash
uv run python -m hangbo.precheck.year_month \
  --input-dir 'data/raw/2026/5月' \
  --year '2026' \
  --month '5'
```

不要对 `1-5月`、`Q1` 这种混合月份目录直接运行这个命令，否则会把所有行都写成同一个月份。

---

## 6. 合并多月数据

推荐顺序是：

```text
先处理/确认每个单月数据
-> 确保每条记录都有正确 年份/月份
-> 合并多月原始数据
-> 跑合并批次主流程
```

例如，要把 `1-4月` 和 `5月` 合并成 `1-5月`：

```bash
uv run python -m hangbo.merge.questionnaire_workbooks \
  --input-dir 'data/raw/2026/1-2月' \
  --input-dir 'data/raw/2026/3月' \
  --output-dir 'data/raw/2026/1-3月'
```

合并完成后，再跑主流程：

```bash
uv run python main_pipeline.py --year 2026 --batch 1-3月
```

合并脚本做的事情：

- 按同名文件合并，例如多个目录里的 `餐饮.xlsx` 合成一个 `餐饮.xlsx`。
- 只读取 `问卷数据` sheet。
- 表头按语义对齐后追加数据行。
- `Q1-调研类别`、`Q2-调研类别` 这类题号前缀会被忽略后对齐。

合并脚本不会做的事情：

- 不去重。
- 不自动修正客户标签。
- 不自动处理标签冲突。
- 不自动判断保留新记录还是旧记录。

---

## 7. 只需要样本统计表时

如果只想合并多个原始目录，并生成客户类型样本统计表，不需要满意度汇总和 PPT，可以运行：

```bash
uv run python merge_sample_summary.py --year 2026
```

运行后按终端提示选择要合并的来源目录，再输入合并后的批次名。

这个脚本会生成：

```text
data/raw/{年份}/{批次}
data/sample_summary/{年份}/{批次}/{批次}客户类型样本统计表.xlsx
```

它不会生成：

```text
满意度分项统计
满意度汇总表
PPT
```

---

## 8. 常用命令模板

### 处理 5 月

```bash
uv run python main_pipeline.py --year 2026 --batch 5月
```

### 处理已经准备好的 1-5 月

```bash
uv run python main_pipeline.py --year 2026 --batch 1-5月
```

### 合并 1-4 月和 5 月为 1-5 月

```bash
uv run python -m hangbo.merge.questionnaire_workbooks \
  --input-dir 'data/raw/2026/1-4月' \
  --input-dir 'data/raw/2026/5月' \
  --output-dir 'data/raw/2026/1-5月'
```

### 单独检查未映射客户标签

```bash
uv run python -m hangbo.precheck.unmapped_customers \
  --input-dir 'data/raw/2026/5月' \
  --log-file 'logs/pipeline/2026/5月/manual_unmapped_check.log'
```

### 手动处理期次列

主流程通常会自动处理。必要时可以单独运行：

```bash
uv run python -m hangbo.precheck.phase_column 'data/raw/2026/5月/展览.xlsx'
```

---

## 9. 容易遗漏的点

- 先处理单月，再合并多月；不要先把脏数据合进累计批次。
- 单月批次会自动补 `年份` / `月份`，合并批次不会。
- 合并批次运行主流程前，必须已经有正确的 `年份` / `月份`。
- 合并脚本只是追加行，不负责去重和修标签。
- 预查错阻断时，先看 `logs/pipeline/{年份}/{批次}` 下的日志。
- PPT 生成前会再次确认，可以在这一步停下来检查 Excel 输出。
- 如果只要样本统计表，用 `merge_sample_summary.py`；如果要完整报告，用 `main_pipeline.py`。
